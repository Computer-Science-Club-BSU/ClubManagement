<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
  <style>

    :root{
    --primary-color: #FFFFFF;
    --accent-color: #89191f;
    --accent-hover-color: #9C2A30;
    --text-color: #000000
    }

    body{
      background-color: #ECEEF1;
    }
    body{
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
    }
    #headerImg{
      width: 100%;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
    }
    #contentField{
      background-color: white;
      width: 50%;
      text-align: center;
      padding: 2rem;
      box-shadow: 0 3px 5px 3px black;
      /*margin-left: 20%;*/
      /*margin-right: 20%;*/
    }
    *{
      /*outline: 1px solid black;*/
      font-family: "Barlow" , sans-serif;
    }
    h2{
      color: #5E798B;
      font-weight: normal;
      font-size: 15pt;
    }
    p{
      color: #192D4B;
    }
    #resetLink{
      padding: 10px 10px 10px 10px;
      background: var(--accent-color);
      color: white;
      text-decoration: none ;
      margin-top: 2rem;
    }
    #about{
      margin-top: 5rem;
      width: 50%;
      text-align: center;
    }
  </style>
</head>
<body>
<div id="headerImg"><img src="data:image/png;base64,{{ imgData }}" alt="CMS" style="width: 1in;"></div>

  <div id="contentField">
  <h2>We received a request to reset your password.</h2>
    <p>Use the link below to set up a new password for your account. If you did not request to reset your password, ignore this email and the link will expire on its own.</p>
    <a href="{{ resetLink }}" id="resetLink">Set New Password</a>
  </div>
<div id="about">
  This email was sent to {{ recp }}<br>
  This password reset link will expire on {{ expDate }} at {{ expTime }}.
</div>
</body>
</html>